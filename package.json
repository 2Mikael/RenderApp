{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../FullStack/part2/NoteApp && npm run build && cp -r dist ../../../RenderApp",
    "deploy": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Mikael Marin",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
